      <div class="custom-container">
            <div class="custom-left-container">
                <h1>What's New</h1>
            </div>
    
            <!-- Swiper Slider Container -->
            <div class="swiper-container custom-right-container">
                <!-- Swiper Wrapper -->
                <div class="swiper-wrapper">
                  {% for block in section.blocks %}
                    <div class="swiper-slide custom-slide">
                      <a href="{{ block.settings.url }}">
                        <img class="custom-image" src="{{ block.settings.image | img_url: 'master' }}" alt="{{ block.settings.heading }}">
                        <h4 class="custom-heading">{{ block.settings.heading }}</h4>
                      </a>
                    </div>
                  {% endfor %}
                </div>
            </div>
        </div>

<style>
/* Default Desktop Styles */
.custom-container {
    display: flex;
    gap: 10px;
    width: 100%;
    max-width: 1400px;
    overflow: hidden;
    margin: 15px auto;
}

.custom-right-container {
    display: flex;
    column-gap: 15px;
    width: 80%;
}

.custom-left-container {
    width: 20%;
    display: flex;
    align-items: center;
    position: relative;
    background: white;
    z-index: 2;
}

.custom-left-container h1 {
    font-family: math;
    padding: 15px;
    width: 100%;
    display: flex;
    align-items: center;
    position: absolute;
    top: 50%;
    background: white;
    height: 435px !important;
    transform: translate(0, -50%);
    z-index: 2;
    justify-content: center;
}

.custom-slide {
    text-align: center;
}

.custom-slide img.custom-image {
    border-radius: 50%;
}

/* Mobile Specific Styles */
@media screen and (max-width: 767px) {
    .custom-container {
        flex-direction: column;
        gap: 20px;
        margin: 10px;
    }

    .custom-right-container {
        width: 100%;
        flex-direction: column;
        column-gap: 10px;
    }

    .custom-left-container {
        width: 100%;
        height: auto;
        position: relative;
        z-index: 1;
        display: flex;
        justify-content: center;
        align-items: center;
        background: white;
    }

    .custom-left-container h1 {
        font-size: 20px; /* Smaller font size for mobile */
        padding: 10px;
        position: relative;
        height: auto;
        transform: translate(0, 0);
        top: auto;
        margin: 10px 0;
        width: 100%;
        height: 100% !important;
    }

    .custom-slide {
        padding: 15px;
    }

    .custom-slide img.custom-image {
        width: 70%; /* Make the image smaller on mobile */
        max-width: 200px;
        margin: 0 auto;
    }
}

/* Tablet Styles */
@media screen and (max-width: 1024px) {
    .custom-container {
        flex-direction: column;
        gap: 15px;
    }

    .custom-left-container h1 {
        font-size: 24px;
        padding: 15px;
    }

    .custom-right-container {
        width: 100%;
        flex-direction: column;
    }

    .custom-slide img.custom-image {
        width: 60%;
    }
}
</style>

{% schema %}
  {
    "name": "VB - Custom What's New",
    "settings": [],
    "blocks": [
      {
        "type": "block",
        "name": "Block",
        "settings": [
          {
            "type": "image_picker",
            "label": "Select Image",
            "id": "image"
          },
          {
            "type": "text",
            "label": "Heading",
            "id": "heading"
          },
          {
            "type": "url",
            "label": "Link",
            "id": "url"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "VB - Custom What's New"
      }
    ]
  }
{% endschema %}